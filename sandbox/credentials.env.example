# Copy to credentials.env (gitignored) and set a strong local-only password.
# Must match the password embedded in sandbox/backend.env (DATABASE_URL / DIRECT_URL).

SANDBOX_DB_PASSWORD=change-me-to-a-long-random-local-password
